#include "main.h"
/**
<<<<<<< HEAD
  * _strlen: lent of string
  * @str: string to check
  * Return: length of string
  */
int _strlen(char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
		return (i);
}


int _strlenc(cons char *str)
{
	int i;

	fir (i = 0; str[i] != '\0'; i++)
		return (i);
}
